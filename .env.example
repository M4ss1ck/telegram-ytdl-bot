# Telegram Bot Configuration
API_ID=your_telegram_api_id
API_HASH=your_telegram_api_hash
BOT_TOKEN=your_telegram_bot_token

# Download Settings
DOWNLOAD_TIMEOUT=600
MAX_FILE_SIZE=2000000000  # ~1.9GB, Telegram limit is 2GB

# YouTube Download Configuration Options

# 1. Proxy Configuration (optional)
# If your server is blocked by YouTube, you can use a proxy:
# PROXY_URL=http://user:pass@proxy_ip:port
# or
# PROXY_URL=socks5://user:pass@proxy_ip:port

# 2. YouTube API Services (optional, but recommended if YouTube is blocked)
# Get a RapidAPI key from: https://rapidapi.com/
RAPIDAPI_KEY=your_rapidapi_key

# 3. Custom YouTube API (optional)
# If you have your own YouTube download API service:
YOUTUBE_API_KEY=your_custom_api_key
YOUTUBE_API_URL=https://your-api-service.com

# 4. Browser Downloader Configuration (optional)
# Browser automation requires playwright or selenium to be installed
# Set to 'true' to enable browser-based downloading:
BROWSER_ENABLED=false
# Choose browser type: 'firefox' or 'chrome'
BROWSER_TYPE=firefox

# 5. YouTube Download Strategy (default: api_first)
# Options:
#   - api_first: Try API services first, then proxy, then browser, then alternative frontends
#   - proxy_first: Try proxy first, then API services, then browser, then alternative frontends 
#   - browser_first: Try browser automation first, then API services, then proxy, then alternative frontends
#   - alt_frontends_first: Try alternative frontends first, then API, then proxy, then browser
YOUTUBE_STRATEGY=api_first

SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret